---
import BaseLayout from '../../layouts/BaseLayout.astro';
import SocialButtons from '../../components/SocialButtons.astro';
import Footer from '../../components/Footer.astro';
import database from '../../data/database.json';

export function getStaticPaths() {
  return database.articles.map(article => ({
    params: { id: article.id.toString() },
    props: { article }
  }));
}

const { article } = Astro.props;
const { id, title, image, description, category, date } = article;

const pageTitle = `${title} - Mi Tienda Online`;
const pageDescription = `${description} Artículo de ${category} publicado el ${new Date(date).toLocaleDateString('es-ES')}.`;
const pageImage = image;

// Related articles (same category, excluding current article)
const relatedArticles = database.articles
  .filter(a => a.category === category && a.id !== id)
  .slice(0, 2);

// If no related articles in same category, get other articles
const otherArticles = relatedArticles.length === 0 
  ? database.articles.filter(a => a.id !== id).slice(0, 2)
  : [];

const displayArticles = relatedArticles.length > 0 ? relatedArticles : otherArticles;
---

<BaseLayout title={pageTitle} description={pageDescription} image={pageImage}>
  <main class="min-h-screen bg-gray-50">
    <!-- Breadcrumb -->
    <nav class="bg-white shadow-sm py-4">
      <div class="max-w-4xl mx-auto px-4">
        <div class="flex items-center space-x-2 text-sm text-gray-600">
          <a href="/" class="hover:text-primary-500 transition-colors duration-300">
            <i class="fas fa-home mr-1"></i>
            Inicio
          </a>
          <i class="fas fa-chevron-right text-gray-400"></i>
          <span class="hover:text-primary-500 transition-colors duration-300 cursor-pointer">
            Artículos
          </span>
          <i class="fas fa-chevron-right text-gray-400"></i>
          <span class="text-primary-500 font-semibold">{category}</span>
          <i class="fas fa-chevron-right text-gray-400"></i>
          <span class="text-gray-800 font-semibold">{title}</span>
        </div>
      </div>
    </nav>

    <!-- Article Content -->
    <article class="py-12">
      <div class="max-w-4xl mx-auto px-4">
        <!-- Article Header -->
        <header class="mb-8">
          <div class="flex items-center gap-3 mb-4">
            <span class="bg-secondary-500 text-white px-3 py-1 rounded-full text-sm font-semibold">
              {category}
            </span>
            <time class="text-gray-500 text-sm">
              <i class="fas fa-calendar-alt mr-1"></i>
              {new Date(date).toLocaleDateString('es-ES', { 
                year: 'numeric', 
                month: 'long', 
                day: 'numeric' 
              })}
            </time>
          </div>
          
          <h1 class="text-3xl md:text-4xl font-bold text-gray-800 mb-6 leading-tight">
            {title}
          </h1>
          
          <p class="text-xl text-gray-600 leading-relaxed">
            {description}
          </p>
        </header>
        
        <!-- Featured Image -->
        <div class="mb-8">
          <img 
            src={image} 
            alt={title}
            class="w-full h-64 md:h-96 object-cover rounded-lg shadow-lg"
            loading="eager"
          />
        </div>
        
        <!-- Article Body -->
        <div class="prose prose-lg prose-gray max-w-none mb-12">
          <div class="bg-white rounded-lg shadow-sm p-8">
            <h2 class="text-2xl font-bold text-gray-800 mb-6">
              <i class="fas fa-newspaper text-secondary-500 mr-2"></i>
              Contenido del Artículo
            </h2>
            
            <div class="space-y-6 text-gray-700 leading-relaxed">
              <p>
                En este artículo exploramos en profundidad el tema de <strong>{title.toLowerCase()}</strong>, 
                proporcionando información valiosa y actualizada para nuestros lectores.
              </p>
              
              <p>
                {description} Este contenido ha sido cuidadosamente investigado y preparado por nuestro 
                equipo de expertos para ofrecerte la información más relevante y útil.
              </p>
              
              <h3 class="text-xl font-semibold text-gray-800 mt-8 mb-4">
                Puntos Clave
              </h3>
              
              <ul class="space-y-3">
                <li class="flex items-start">
                  <i class="fas fa-check-circle text-green-500 mr-3 mt-1"></i>
                  <span>Información actualizada y verificada por expertos</span>
                </li>
                <li class="flex items-start">
                  <i class="fas fa-check-circle text-green-500 mr-3 mt-1"></i>
                  <span>Contenido relevante para la categoría {category}</span>
                </li>
                <li class="flex items-start">
                  <i class="fas fa-check-circle text-green-500 mr-3 mt-1"></i>
                  <span>Aplicación práctica en la vida cotidiana</span>
                </li>
                <li class="flex items-start">
                  <i class="fas fa-check-circle text-green-500 mr-3 mt-1"></i>
                  <span>Tendencias y novedades del sector</span>
                </li>
              </ul>
              
              <h3 class="text-xl font-semibold text-gray-800 mt-8 mb-4">
                Conclusión
              </h3>
              
              <p>
                Este artículo sobre {title.toLowerCase()} proporciona una visión completa del tema, 
                ayudándote a mantenerte informado sobre las últimas tendencias y desarrollos en {category}.
              </p>
              
              <div class="bg-gray-50 rounded-lg p-6 mt-8">
                <h4 class="text-lg font-semibold text-gray-800 mb-3">
                  <i class="fas fa-lightbulb text-accent-500 mr-2"></i>
                  ¿Te ha resultado útil este artículo?
                </h4>
                <p class="text-gray-600 mb-4">
                  Compártelo con tus amigos y síguenos en nuestras redes sociales para más contenido como este.
                </p>
              </div>
            </div>
          </div>
        </div>
        
        <!-- Social Buttons -->
        <div class="bg-white rounded-lg shadow-sm p-6 mb-12">
          <h3 class="text-xl font-semibold text-gray-800 mb-4 text-center">
            <i class="fas fa-share-alt text-primary-500 mr-2"></i>
            Compartir Artículo
          </h3>
          <SocialButtons />
        </div>
      </div>
    </article>
    
    <!-- Related Articles -->
    {displayArticles.length > 0 && (
      <section class="py-16 bg-white">
        <div class="max-w-6xl mx-auto px-4">
          <h2 class="text-3xl font-bold text-center mb-12 text-gray-800">
            <i class="fas fa-newspaper text-secondary-500 mr-2"></i>
            Artículos Relacionados
          </h2>
          
          <div class="grid md:grid-cols-2 gap-8">
            {displayArticles.map(relatedArticle => (
              <article class="bg-white rounded-lg shadow-lg overflow-hidden hover:shadow-xl transition-shadow duration-300">
                <div class="relative">
                  <img 
                    src={relatedArticle.image} 
                    alt={relatedArticle.title}
                    class="w-full h-48 object-cover"
                    loading="lazy"
                  />
                  <div class="absolute top-4 left-4 bg-secondary-500 text-white px-3 py-1 rounded-full text-sm font-semibold">
                    {relatedArticle.category}
                  </div>
                </div>
                
                <div class="p-6">
                  <time class="text-gray-500 text-sm">
                    {new Date(relatedArticle.date).toLocaleDateString('es-ES', { 
                      year: 'numeric', 
                      month: 'long', 
                      day: 'numeric' 
                    })}
                  </time>
                  <h3 class="text-xl font-bold text-gray-800 mt-2 mb-3">{relatedArticle.title}</h3>
                  <p class="text-gray-600 mb-4">{relatedArticle.description}</p>
                  
                  <a 
                    href={`/articulo/${relatedArticle.id}`}
                    class="inline-flex items-center text-primary-500 hover:text-primary-600 font-semibold transition-colors duration-300"
                  >
                    Leer más
                    <i class="fas fa-arrow-right ml-2"></i>
                  </a>
                </div>
              </article>
            ))}
          </div>
          
          <div class="text-center mt-12">
            <a 
              href="/#articulos" 
              class="bg-secondary-500 hover:bg-secondary-600 text-white px-8 py-3 rounded-lg font-semibold transition-colors duration-300 inline-flex items-center"
            >
              <i class="fas fa-newspaper mr-2"></i>
              Ver Todos los Artículos
            </a>
          </div>
        </div>
      </section>
    )}
  </main>
  
  <Footer />
</BaseLayout>

<style>
  .prose h2 {
    color: #1f2937;
    font-weight: 700;
  }
  
  .prose h3 {
    color: #374151;
    font-weight: 600;
  }
  
  .prose p {
    margin-bottom: 1.5rem;
  }
  
  .prose ul {
    margin: 1.5rem 0;
  }
</style>